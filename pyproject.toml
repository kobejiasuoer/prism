[project]
name = "prism"
version = "0.1.0"
description = "Full-source AI-native investment research system"
requires-python = ">=3.14"

[tool.pytest.ini_options]
testpaths = ["apps/control-panel/tests", "tests"]
pythonpath = [".", "apps/control-panel", "packages"]
